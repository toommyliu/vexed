---
title: CLI Args
description: Supported CLI options
---

## Game Options

Launches the app with the game.

### --game, -g

```bash
--game, -g
```

Launches a game window.

### Additional Game Options

The following options are available launching with the game window:

#### --username

```bash
--username="username"
```

Specifies the username for authentication.

#### --password

```bash
--password="password"
```

Specifies the password for authentication.

#### --server

```bash
--server="server"
```

Specifies the server to connect to.

#### --scriptPath

```bash
--scriptPath="/path/to/script.js"
```

Specifies the path to a script file to execute.

## Manager Options

### --manager, -m

```bash
--manager, -m
```

Launches the app with the account manager.
