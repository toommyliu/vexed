---
title: Bot
---

import { Badge, Code } from "@astrojs/starlight/components";

## Properties
### army
The army API class instance.

Type: [`Army`](/api-legacy/army)

### auth
The Auth API class instance.

Type: [`Auth`](/api-legacy/auth)

### autoRelogin
The AutoRelogin API class instance.

Type: [`AutoRelogin`](/api-legacy/util/autorelogin)

### bank
The Bank API class instance.

Type: [`Bank`](/api-legacy/bank)

### combat
The Combat API class instance.

Type: [`Combat`](/api-legacy/combat)

### drops
The Drops API class instance.

Type: [`Drops`](/api-legacy/drops)

### flash
The Flash API class instance.

Type: [`Flash`](/api-legacy/util/flash)

### house
The House API class instance.

Type: [`House`](/api-legacy/house)

### inventory
The Inventory API class instance.

Type: [`Inventory`](/api-legacy/inventory)

### packets
The Packets API class instance.

Type: [`Packets`](/api-legacy/packets)

### player
The local Player API class instance.

Type: [`Player`](/api-legacy/player)

### quests
The Quests API class instance.

Type: [`Quests`](/api-legacy/quests)

### settings
The Settings API class instance.

Type: [`Settings`](/api-legacy/settings)

### shops
The Shops API class instance.

Type: [`Shops`](/api-legacy/shops)

### tempInventory
The TempInventory API class instance.

Type: [`TempInventory`](/api-legacy/tempinventory)

### world
The World API class instance.

Type: [`World`](/api-legacy/world)

## Methods

### sleep
Pauses execution for a specified amount of time.
| Parameter | Type | Description |
|-----------|------|-------------|
| ms | `number` | The number of milliseconds to wait. |

**Returns**: `Promise<void>`

### waitUntil
Waits until the condition is met.
| Parameter | Type | Optional | Default | Description |
|-----------|------|----------|---------|-------------|
| condition | `() => boolean` |  |  | The condition to wait for until it returns true. |
| prerequisite | `null` \| `(() => boolean)` | ✓ | `null` | The prerequisite to be checked before waiting for the condition. |
| maxIterations | `number` | ✓ | `15` | The maximum number of iterations to wait. -1 to wait indefinitely. |

**Returns**: `Promise<void>`

### getInstance <Badge text="static" />
Gets the singleton instance of the Bot class.

**Returns**: [`Bot`](/api-legacy/bot)

## Events

### afk
This event is emitted when the player goes AFK.

### login
This event is emitted when the player logs in.

### logout
This event is emitted when the player logs out.

### monsterDeath
This event is emitted when a monster has died.

| Parameter | Type | Description |
|-----------|------|-------------|
| monMapId | `number` | The monster map id. |

### monsterRespawn
This event is emitted when a monster has respawned.

| Parameter | Type | Description |
|-----------|------|-------------|
| monster | [`Monster`](/api-legacy/models/monster) | The monster that has respawned. |

### packetFromClient
| Parameter | Type | Description |
|-----------|------|-------------|
| packet | `string` | The packet dispatched from the client. |

### packetFromServer
| Parameter | Type | Description |
|-----------|------|-------------|
| packet | `string` | The packet received from the server. |

### pext
OnExtensionResponse event.

| Parameter | Type | Description |
|-----------|------|-------------|
| packet | `Record<string, unknown>` |  |

### playerDeath
This event is emitted when a player dies.

| Parameter | Type | Description |
|-----------|------|-------------|
| playerName | `string` | The name of the player who died. |

### playerJoin
This event is emitted when a player joins the room.

| Parameter | Type | Description |
|-----------|------|-------------|
| playerName | `string` | The name of the player. |

### playerLeave
This event is emitted when a player leaves the room.

| Parameter | Type | Description |
|-----------|------|-------------|
| playerName | `string` | The name of the player. |
