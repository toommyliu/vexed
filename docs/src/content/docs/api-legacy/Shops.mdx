---
title: Shops
---

import { Badge, Code } from "@astrojs/starlight/components";

## Properties
### bot

Type: [`Bot`](/api-legacy/bot)

### info <Badge text="Getter" size="small" />	
The info about the current loaded shop.

Type: `null` \| [`ShopInfo`](/api-legacy/typedefs/shopinfo)

### isMergeShop <Badge text="Getter" size="small" />	
Whether the shop is a merge shop.

Type: `boolean`

## Methods

### buyById
Buy an item from the shop.
| Parameter | Type | Description |
|-----------|------|-------------|
| itemId | `number` | The id of the item. |
| quantity | `number` | The quantity of the item. |

**Returns**: `Promise<void>`

### buyByName
Buy an item from the shop.
| Parameter | Type | Description |
|-----------|------|-------------|
| itemName | `string` | The name of the item. |
| quantity | `null` \| `number` | The quantity of the item. If not provided, it will default to 1. |

**Returns**: `Promise<void>`

### canBuyItem
Whether an item can be bought from the shop, works for both normal and merge shops.
| Parameter | Type | Description |
|-----------|------|-------------|
| itemName | `string` | The name of the item. |

:::note
This operation performs client-side checks only. The final validation is done server-side.
- Upgrade status
- Faction reputation
- Class points
- Quest status
- Sufficient gold
- Sufficient ACs
- Sufficient inventory / house space
:::

**Returns**: `boolean`

### getById
Get a shop item by its ID.
| Parameter | Type | Description |
|-----------|------|-------------|
| itemId | `number` | The ID of the item. |

**Returns**: `null` \| [`ShopItem`](/api-legacy/models/shopitem)

### getByName
Get a shop item by its name.
| Parameter | Type | Description |
|-----------|------|-------------|
| itemName | `string` | The name of the item. |

**Returns**: `null` \| [`ShopItem`](/api-legacy/models/shopitem)

### isShopLoaded
Whether any shop is loaded.

**Returns**: `boolean`

### load
Load a shop.
| Parameter | Type | Description |
|-----------|------|-------------|
| shopId | `string` \| `number` | The shop ID. |

**Returns**: `Promise<void>`

### loadHairShop
Loads a hair shop.
| Parameter | Type | Description |
|-----------|------|-------------|
| shopId | `string` \| `number` | The shop ID. |

**Returns**: `void`

### openArmorCustomizer
Opens the Armor Customization menu.

**Returns**: `void`

### sell
Sells an entire stack of an item.
| Parameter | Type | Description |
|-----------|------|-------------|
| key | `string` | The name or ID of the item. |

**Returns**: `Promise<void>`
