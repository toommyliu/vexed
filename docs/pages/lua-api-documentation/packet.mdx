## packet:send()

```lua
void packet:send(string packet, string type = "String")
```

## packet:send_client()

```lua
void packet:send_client(string packet, string type = "str")
```

## packet:send_msg()

```lua
void packet:send_msg(string msg, string sent_by, string type)
```

## packet:whisper()

```lua
void packet:whisper(string name, string msg)
```

## packet:send_server()

```lua
void packet:send_server(string msg, string sent_by = "arcana")
```

helper for packet:send_msg(msg, sent_by, "server")

## packet:send_moderator

```lua
void packet:send_moderator(string msg, string sent_by = "arcana")
```

helper for packet:send_msg(msg, sent_by, "moderator")

## packet:send_warning

```lua
void packet:send_warning(string msg, string sent_by = "arcana")
```

helper for packet:send_msg(msg, sent_by, "warning")

## packet:send_event

```lua
void packet:send_event(string msg, string sent_by = "arcana")
```

helper for packet:send_msg(msg, sent_by, "event")

## packet:send_guild

```lua
void packet:send_guild(string msg, string sent_by = "arcana")
```

helper for packet:send_msg(msg, sent_by, "guild")

## packet:send_whisper

```lua
void packet:send_whisper(string msg, string sent_by = "arcana")
```

helper for packet:send_msg(msg, sent_by, "whisper")
